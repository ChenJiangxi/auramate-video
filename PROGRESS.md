# PROGRESS — 建设记录

**这份文件是历史，不是现状。** 想知道现在能干什么、还差什么，读 `README.md`。
留着它是因为里面每一条「自测记录」都写了**当时是怎么发现问题的** ——
下一个 agent 与其重走一遍弯路，不如先看别人在哪儿摔过。

## 现状（2026-08-17）

13 个 skill · 33 个 lib 脚本 · 5 套卡模板 · 4 篇 references ·
2 个零 key 可跑样例 · `tests/validate.sh` **152 条断言**（`--skip-render` 51 条）。

完成条件全部满足：子 skill 齐全 / validate 全绿 / 零 context agent 干净克隆无 key
走到 `AUDIT PASS` / 已推送 / `SECRETS-CHECKLIST.md` 已交付。

**还没验证的只剩一项**：克隆音（`voice_id` 私有）没测过，只测了三个系统音色。
其余都在真实条件下跑过 —— 真 MiniMax 接口、真产品站点登录录屏、真素材、真交付回测。
最新的一份诚实清单以 `README.md` 末节为准（那里会跟着改，这里不会）。

---

## Roadmap

### ✅ Run 1 — 骨架 + 主干 + 可验证性（2026-08-10）

- [x] repo 骨架：README / INSTALL(install.sh) / .gitignore / PROGRESS
- [x] `skills/video-master/` 总纲：硬规矩 H1–H11、路由表、8 阶段管线、验收清单、症状表
- [x] `skills/vertical-shortform/` 竖版主干管线（audio-driven、4 个编码器、concat、音轨防漂移）
- [x] `lib/build-vertical.sh` 参数化合成（clips.json + shots.tsv 驱动）
- [x] `lib/gen-subs.py` / `lib/burn-subs.sh` 字幕（PIL overlay 路线）
- [x] `lib/gen-voice.mjs` 配音（含音色采样模式、报错码提示）
- [x] `lib/verify-audio.sh` / `lib/verify-output.sh` 机械自检
- [x] `lib/init-project.sh` / `lib/package-delivery.sh` / `lib/make-cover.py` / `lib/make-brand-assets.py`
- [x] `lib/rec-page.js` / `lib/render-card.js`
- [x] 其余 8 个子 skill 首版（tts-voiceover / subtitles / real-clip-mashup / product-demo /
      html-motion-cards / cover-thumbnail / delivery / ffmpeg-cookbook）
- [x] `references/bilibili-longform.md`
- [x] `tests/check-deps.sh` + `tests/validate.sh`（结构 / 死链 / 语法 / 泄密扫描 / 端到端渲染）
- [x] `examples/hello-vertical/` 零 key 可跑样例
- [x] `SECRETS-CHECKLIST.md`

### ✅ Run 1.5 — 合规红线（2026-08-10，Jessy 现场提出，插队做）

- [x] `skills/compliance-redlines/` —— 一句话原则「宣扬封建迷信必违规；讲 AI 算命、
      讲产品功能不违规」+ 七类必删 + 高风险清单 + 四个安全表达框（技术/文化/产品/心理）
      + 逐词替换表 + 交付前三问
- [x] `lib/check-compliance.py` —— BLOCK / WARN / INFO 三档 linter，扫 clips.json +
      topic.md + caption.txt，有 BLOCK 退出码 1
- [x] `tests/fixtures/compliance-{bad,good}/` + validate.sh 断言（违规样本必须被拦、
      合规样本不许误报、样例工程自身必须合规）
- [x] 接进主干：video-master §3 合规红线（优先级高于硬规矩）、vertical-shortform
      阶段①②⑧ 三处闸门、delivery 交付前检查、README

### ✅ Run 2 — 选题与脚本（2026-08-10）

- [x] 新 skill `skills/topic-and-script/`：四框角度库（12 条真实交付选题拆解）、
      6 个已验证钩子句式 + 具体性自检、clips.json 分句规则、结构时间预算、
      平台文案模板、常见废稿特征、失败症状表
- [x] 选题阶段就落「四个安全表达框」，`topic.md` 模板加了「安全框」一节
- [x] `lib/check-script.py`：单拍时长 / 单句字数 / `ta` / 钩子具体性 / 末句收品牌 /
      句式重复 / 框定话术，硬错误退出码 1
- [x] `references/caption-template.md`：真实交付格式 + 样例 + 三条规矩
- [x] `examples/hello-vertical/topic.md` 补成真样例（含镜头表）
- [x] **实测语速**替换掉拍脑袋的 5 字/秒（video-master / vertical-shortform 都改了）

（角度库目前是 12 条真实做过的拆解，不是「每框 10 个待做选题」——
待做选题清单留到 Run 8 收尾时补，先把已验证的沉淀下来更有价值。）

### ✅ Run 3 — real-clip-mashup 深化（2026-08-10）

- [x] 四步工作流（检索 → 体检 → 挑起始点 → 竖版化），体检那步不许跳
- [x] `lib/fetch-clip.sh`：`search` / `get` / `probe` 三个子命令。强制 h264、站点 UA+Referer
      预设、落地自动体检（画幅/编码/时长/放大倍数）+ 抽 9 帧拼 contact 图看水印
- [x] `lib/fit-vertical.sh`：任意画幅 → 1080×1920。auto 选 blur/fill/pad，按 `--max-upscale`
      自动缩前景防糊，前景中心 40% 高度、底边不压字幕，素材不够长直接报错并告知差多少
- [x] 竖版化参数查表：放大倍数阶梯 + 三类源的前景尺寸/落点，全部从真实素材库量出来
- [x] 检索关键词构造表、干净来源经验、编码现实（真实库 h264×30 / av1×4 / hevc×1）

### ✅ Run 4 — product-demo 深化（2026-08-10）

- [x] 「盘点 → 缺口清单 → 只补缺口」流程 + 按分辨率分三档判断素材还能不能用
- [x] `lib/zoom-crop.sh`：`--auto`（cropdetect）/ `--zoom`（按倍数居中，可移中心）/
      `--crop`（精确框）/ `--grid`（导带坐标网格的样帧——agent 看不见画面，这是唯一靠谱的量法）
- [x] `lib/browser-chrome.py`：PIL 渲染假 Mac 浏览器壳（红黄绿灯 + 锁 + URL 药丸，明暗两套）
- [x] `lib/wrap-chrome.sh`：壳 + 内容 vstack，内置 `-loop 1` 必配 `-t` 的保护和
      `--freeze` 冻结末帧
- [x] **录制分辨率倒推表** —— 裁切倍数和升采样倍数是相乘的，这笔账以前没算清楚

### ✅ Run 5 — html-motion-cards 深化（2026-08-10）

- [x] 5 套模板实文件：`hook-slam` / `question` / `bar-rank` / `stat-compare` / `cta-brand`
- [x] `_base.css` 公共设计系统：色/字号阶梯/氛围三层（glow→stars→vig）/动画与延时链，
      从真实交付过的几十张卡抽出来的
- [x] `lib/storyboard.js`：storyboard.json 填模板 → 渲染 → **直接产出 shots.tsv**，
      时长默认走 audio-driven（跟 build-vertical 同一套规则，不会漂）；`--no-render` 不需要浏览器
- [x] 极简模板引擎（`{{key}}` + `<!-- repeat:rows -->`），数组自动派生 `rowsCount`/`rowsH`

### ✅ Run 6 — 质量闸门与自审（2026-08-10）

- [x] `skills/quality-gate/`：把「机器能判的」和「只有人能判的」明确分成两列，
      外加**返工成本排序**（选题错最贵→参数错最便宜，所以闸门要前置）
- [x] `lib/audit-video.sh`：八项机械检查一条命令跑完（脚本/合规/配音/分镜/画面构成/
      字幕/成片/交付），硬失败退出码 1
- [x] **被打回案例库**：6 大类 17 条真实案例（症状 → 反馈原话 → 改法），
      素材来自交付包里的 v2/v3/v4 修订记录和历次反馈
- [x] 画面构成统计：按 `footage/ext` / `footage/rec` / `html/beats` 目录约定
      算卡片 / 真实切片 / 产品录屏各占多少时长

### ✅ Run 7 — 零 context 冒烟测试（2026-08-10）

- [x] 干净目录 + 干净克隆 + **零 API key**，从 `init-project` 走到 `AUDIT PASS`
- [x] 卡壳 5 处全部回填（见下方自测记录 + `references/zero-context-walkthrough.md`）
- [x] `lib/make-placeholders.sh` + `lib/_fix_shots.py`：任意工程都能造占位配音/素材，
      并补齐 shots.tsv 缺的拍
- [x] `tests/validate.sh` 新增「零 context 冒烟」常驻断言 —— 这条路径以后坏了会立刻红

### ✅ Run 8 — 收尾（2026-08-10）

- [x] `tests/check-consistency.py`：孤儿脚本 / 路由完整 / README 覆盖 /
      **旧说法不许复活**，已接进 `validate.sh`
- [x] 修掉扫出来的 4 处旧口径 + 2 处路由缺失
- [x] README 加「我要做 X → 读哪几个文件」按角色导航（15 条）
- [x] 把 Run 3–7 踩的新坑回填进 `vertical-shortform` 的失败症状表

---

### ✅ Run 9 — 逻辑链 / 人味儿 / 真 demo（2026-08-10～11）

- [x] 文案先写**逻辑链**再填字（三道 linter 全绿也可能是流水账，
      机器查不了「这句接不接得住上一句」）→ `topic-and-script` §5.5
- [x] `lib/check-humanness.py` 人味儿闸门；`lib/rec-frames.js` 高像素录屏
      （布局用窄 viewport，像素用 dsf）；`lib/motion.sh` 五种镜头运动
- [x] README 放真 mp4 demo（走 jsDelivr，`raw.githubusercontent` 的
      `application/octet-stream` 播不了）

### ✅ Run 10 — 转场 + 光标（2026-08-11）

- [x] 转场：默认 `fade`，落在上一拍句末的 GAP 静音里 —— 总长不变、每句开口那一帧
      和硬切版一致、音频不用交叉淡化。shots.tsv 第 7 列逐拍指定
- [x] 光标：`lib/cursor-overlay.js` + `lib/actor.js`，动作表按**帧**驱动
      （逐帧截图的墙上间隔不均匀，CSS 动画会忽快忽慢）
- [x] **顺带挖出音画逐拍漂移**：每拍 `-t` 向上取整到整帧，concat 起来累加，
      真片子末拍画面晚 171ms 且全程无报错。改成按绝对边界取整，降到 17ms（半帧）
- [x] `tests/transitions-test.sh` + `tests/cursor-test.js`，自检 85 → 116 条

### ✅ Run 11 — 光标进片子 + 整片运镜检查（2026-08-12）

- [x] 重录素材：真站点登录 + `--cursor` + `--act`，手指点大运/流年，命盘跟着多出一列
      —— 之前工具做了但 demo 用的是老素材，等于没做
- [x] `lib/check-motion.py`：**整片**镜头检查（起手位置 / 方向分布 / 推拉配比 /
      有没有拍其实没动）。调 `motion.sh --dry-run` 拿真实框，不重写算法。
      拿旧 demo 自测，一次抓出「8 拍里 6 拍从全屏起手」
- [x] `motion.sh --lead`：转场多渲的那 0.233s 不许把镜头路径时序带偏
- [x] 转场时长取整到帧（0.22s @30fps = 6.6 帧，源会被采样在两帧之间）
- [x] **想让录屏字更大该调视口不是裁**：输出和源都是 9:16，裁框必然是竖长条，
      硬裁正文会切字。视口 720→440 让浏览器重排，同一段字占画幅大 1.6 倍

### ✅ Run 12 — 强调层（该看哪）+ 桌面录屏（2026-08-15，Jessy 提出）

起因是 Jessy 问「录屏能不能加个光标指示位置或者高亮」。光标 Run 10/11 就做完了，
**高亮没有**；而且真去看她最近那批 16:9 成片，一根光标也没有 —— 因为那批走的是
`rec-page.js`（`recordVideo`，插不进逐帧状态），根本不在有光标的那条路上。

- [x] `lib/cursor-overlay.js` 加强调层：`spot` 暗场聚光 / `ring` 高亮框 /
      `mark` 记号笔 / `label` 标注气泡。和光标共用一个叠加层，同样**按帧**驱动
- [x] `lib/actor.js` 加 5 个动作（含 `clear`），公共参数 `over/hold/out/pad/r/color/id`
- [x] **强调框每帧重新量目标位置** —— 页面还在滚、图片刚加载完撑高了，框都得跟着走。
      钉在原地的话旁白说「看这里」，框已经落在别的元素上了
- [x] 两块暗场叠在一起会互相把对方那块也压暗 → 新 `spot` 进来，旧的自动退场
- [x] `mark` 只盖文字下半截（整块盖住字就糊，观众是来读界面的）
- [x] 目标在视口外整个不画并警告（否则暗场的洞跑到屏幕外 = 成片整屏全黑）
- [x] `--no-cursor` 只收手指，强调照画 —— 两者共用一层，早期一关就全没了
- [x] **`lib/rec-frames.js --desktop`**：1440×900 · dsf 2 · 桌面 UA · 箭头光标。
      不加这条，16:9 产品片根本用不上动作表 —— rec-frames 一直强制移动端仿真，
      网站会切成手机布局，录出来不是那个界面。桌面画面里出现手指也是穿帮的
- [x] `tests/highlight-test.js` 20 条断言 · `tests/_pngdiff.js` 抽出共用 PNG 解码器
- [x] validate.sh 123 → **146 条**，全绿

**这一轮的两个坑都在测试自己身上，代码是对的**（差点因此错判）：

1. 「洞跟着目标走」第一版拿**滚动前**的底图比滚动后的画面 —— 量到的是内容变化，
   不是强调有没有跟上。改成在同一滚动位置先截一张裸底再比。
2. 「目标滚出视口要警告」第一版写 `scrollTo(0,1400)`，而 fixture 总高只比视口多 342px，
   根本滚不到，元素还在视口里 —— 警告不响是对的。改成滚到底 + 换个更靠上的元素。

两条都是「测试红了先怀疑代码」的反例：**先把被测状态打出来看一眼**，
两次都是三分钟就定位到测试自己写错。

### ✅ Run 13 — README demo 换横版（2026-08-15，Jessy 打回竖版）

原话：「特别是竖版，打开显得视频里的字好大，不用放大成这样吧。还是横版给个 demo 吧，
就用带光标的。」抽帧证实：竖版 demo t=13s 一个「酉」字占了半屏。
根因是**两条规矩叠着吃** —— 源已经是 440 窄视口（浏览器把字排大了），后期又推 2.4×。

- [x] 新 demo：30.6s · 1920×1080 · 真配音 · 字幕 · 无 BGM，素材是
      `auramate.net/bazi-ganzhi` 命盘页的桌面录屏。七拍讲的就是这套工具本身：
      箭头移到「日主 己（土）」→ 套框 + 气泡 → 日柱那列暗场聚光 → 记号笔 → 真点下去
- [x] **镜头收着**：最大 1.4×，七拍里四拍完全不动。运动只用来带路
      （`locate` 跟光标走、`pull-out` 收尾）。该看哪交给强调层
- [x] `README.md` 顶部换成横版；竖版降成一行链接（逻辑链那段跟着它走，它演示的是文案）
- [x] 新增第十条反直觉结论：「放大也会过头，而且是双重的」
- [x] `skills/product-demo/` 补 §二·0「先定画幅」：`--desktop` 参数表 +
      三条跟着来的规矩；失败症状表 +4 行
- [x] `docs/demo/purge-cdn.sh` 补新文件名，推送后已 purge，jsDelivr 返回 `video/mp4`

**这一轮踩的三条**（都进了 product-demo 的症状表）：

1. **横版视口自己就得是 16:9。** `--desktop` 默认 1440×900 是 16:10，
   直接 `scale=1920:1080` 是横向拉伸 —— 她之前那批 16:9 成片就是这么编的。
   改成 `--w 1440 --h 810`，缩下去才是纯降采样。
2. **强调别落在画面最底下那一行。** 1080 高的成片字幕带在 y≈924–982，
   第一版 b5 推到底部那行干支关系，高亮和气泡整个被字幕糊住，重编。
3. **录之前页面状态要干净。** 第一条录屏拍到的是上一轮探路点出来的对话残留，
   整屏一段无关文字，废了重录 —— 产品是有状态的，探路会改状态。

### ✅ Run 14 — 拆一条同赛道爆款，补上标注层（2026-08-16，Jessy 丢了个抖音链接）

Jessy：「你要学习一下真人剪辑视频的一些技能，为什么他们会火。文案是怎么样的，
来优化一下你的 skill。」给的是 shanyi / FormSight「我把自己模拟出了3条人生线」，5.5 万赞。

- [x] **真把片子扒下来拆了**：抖音要 cookie，`yt-dlp` 直接下不动 →
      用自管 Chrome 打开作品页拿 cookie + 文案 + 元数据，再 `yt-dlp --cookies` 下片，
      逐帧把口播稿和画面红字抄了一遍。拆解落在 `references/douyin-case-formsight.md`
- [x] **补上真缺的那一层**：`lib/gen-callouts.py` —— 大字标注（3–8 字重点词，
      中上部，红字白描边）。原来只有字幕层，没有标注层
- [x] `burn-subs.sh --manifest` 改成可给多次：字幕 + 标注**两层一次压完，只编码一遍**
- [x] `skills/topic-and-script/`：钩子库加第 7 式**否定式排除**（「不是星座/不是MBTI/不是算命」）；
      §六 补「30 秒也成立」的实测点；§七 补「结构化枚举」；§八 补「正文补位 + CTA 三处一致 + 平台差异」
- [x] `skills/video-master/`：H14 双层文字、H15 **什么时候不要套浏览器壳**
- [x] `skills/product-demo/`：假浏览器壳那节加前置判断
- [x] validate.sh +4 条（标注渲染、两层烧录、产物真的变了）

**这条为什么火 —— 拆出来六条，前两条最值钱**：

1. **否定式排除**。「不是星座 / 不是MBTI / 不是算命」一句话干三件事：把自己从迷信里摘出去、
   借走三个大词的注意力、逼观众想「那你是什么」。auramate 处境一模一样，这个句式直接能用。
2. **结构化枚举**：默认线 / 转向线 / 断裂线，一条一句。观众能记住、能复述、
   **能在评论区认领自己**（「我应该是断裂线吧」）—— 互动就是这么来的，罗列功能做不到。
3. 双层文字：大字管完播，字幕管听不清。4. CTA 三处一致（口播 + 画面 + 正文）。
5. 原始感 > 精致：Windows Chrome 窗口连地址栏都露着，反而可信。
6. 横版 + 30 秒也能跑，前提是画面信息密度高。

**没照抄的**：他的开源叙事是身份红利，auramate 是商业产品硬套会翻车 ——
我们对应的资产是有官方标准答案的评测数据。

### ✅ Run 15 — 录屏节奏：真去录了灵伴三条（2026-08-17，Jessy 提出）

Jessy：「我觉得主要是录屏的节奏要对，你尝试录屏一次灵伴的看看。」录了三条，
**三条都不合格**，但每条都问出了新东西。

- [x] `lib/check-rhythm.py`：把每一秒画面变了多少摊成条形图 + 判据。
      两套模式 —— `scroll`（报告页，看动占比）/ `event`（对话型，看事件数和事件间隔）
- [x] `lib/actor.js`：`wheel` 默认改成 **smoothstep 缓动**（匀速滚是机械感的根源）；
      新增 `key` 动作（回车提交，以前只能去猜发送按钮的选择器）
- [x] `skills/product-demo/` 二·8「节奏：先问这个产品有没有发动机」+ `video-master` H16

**三条录屏问出来的**：

| 版本 | 排法 | 动占比 | 事件数 | 结果 |
|---|---|---|---|---|
| v1 28s | 报告页节奏：定住 → 滚 → 停 → 滚 | 10% | 0 | 三次滚动**全落空** |
| v2 21s | 事件驱动：打字 → 回车 → 回答 | 10% | 1 | 只有一个尖峰 |
| v3 23s | 双事件 + 强调层造节拍 | 17% | 5 | 只剩「8–15s 死时间」一条 |

**结论：灵伴没有"节奏发动机"。** 能推着画面走的只有三种（可滚的长内容 / 内容在生长 /
人在操作），它三样都很弱 —— 对话撑不满一屏所以没得滚，回答是**一整段瞬间出现**
（前端没做逐字流式）所以拍不到"生长"，只剩打字是真动作。
这不是参数问题，是**选错了要录的页面**。

**两个被数据推翻的假设**（都写进文档了）：

1. 「连续滚太久不行」是错的。拿那条 5.5 万赞量：**连续滚 18 秒**照样好看，
   靠的是滚得有快有慢（峰值/均值 1.58）。要卡的是"又长又匀速"，不是"长"。
2. **度量方法差点让我判反。** 第一版用「平均像素差」，灵体那个粒子球一直在动，
   把底噪抬到 1.5 —— 结果「一屏死内容 + 一个动画球」被判成"全程都在动"。
   改成数**真的变了的像素占比**（灰度差 >12），底噪掉到 0.00，死片子立刻现原形。

## 每轮自测结论

| Run | 日期 | validate.sh | 端到端渲染 | 备注 |
|---|---|---|---|---|
| 1 | 2026-08-10 | ✅ OK | ✅ 1080×1920 / 11.35s / h264+aac / 30fps | 见下方 Run 1 自测记录 |
| 1.5 | 2026-08-10 | ✅ OK | ✅ 同上（未回归） | 合规 linter 三项断言全过；历史真实口播稿回测无误报 |
| 2 | 2026-08-10 | ✅ OK | ✅ 1080×1920 / 11.35s / h264+aac / 30fps | 脚本 linter 三项断言全过；回测揪出我自己定错的 3 条规则 |
| 3 | 2026-08-10 | ✅ OK | ✅ 同上 + fit-vertical 四画幅各出片 | 两个新脚本都在真实素材上跑过；contact 图当场抓出一条带下载广告的素材 |
| 4 | 2026-08-10 | ✅ OK | ✅ 同上 + 产品录屏工具 9 条断言 | 三个新脚本都在真实录屏上跑过；抽帧目视确认浏览器壳正确 |
| 5 | 2026-08-10 | ✅ OK | ✅ 同上 + 5 张卡真渲染 1080×1920 | 抽帧目视揪出 2 个模板 bug 并修掉 |
| 6 | 2026-08-10 | ✅ OK | ✅ 同上 + audit 三条断言 | audit 在样例工程上真跑；揪出 build-vertical 一个路径 bug |
| 7 | 2026-08-10 | ✅ OK | ✅ 同上 + 零 key 全链路出片 1080×1920 | 干净克隆真走一遍，揪出 5 处「文档看着通、真跑就断」 |
| 8 | 2026-08-10 | ✅ OK | ✅ 全链路未回归 | 一致性检查上线并自证能抓回归；扫出 6 处矛盾全修 |

### Run 8 自测记录（收尾）

**机械扫出来的 6 处矛盾**（不是感觉，是脚本报的）

1–4. **旧口径没跟着改**：`video-master` 验收清单还写「竖版 60–90s」（正文已是 40–60s）、
`README` 和 `vertical-shortform` 的 frontmatter 也还是 60–90s、
`tts-voiceover` 还把「单句 ≤ 30 字」当硬上限写（Run 2 已实测推翻）、
`vertical-shortform` 的 checklist 同样。全部改掉。

5–6. **路由缺失**：`video-master` 没有指向 `cover-thumbnail` 和 `ffmpeg-cookbook`，
agent 从总纲到不了这两个 skill。已把「一定会用上的 skill」改成带说明的表格，7 个全列进去。

**新增的常驻防线**

`tests/check-consistency.py` —— 最有价值的是第 ④ 条「**已推翻的说法不许复活**」：
这个 repo 的数值是一轮轮实测改出来的（语速 5→6.35、30 字上限被推翻、末句 15 字被推翻），
旧说法留在别处就会误导下一个 agent。讲历史的文件（PROGRESS / 走查记录）和
明确写着「被推翻」的行自动豁免。

**自证有效**：临时往 `delivery/SKILL.md` 塞了一行「目标时长 60–90s，单句 ≤30 字。」，
检查器立刻报出 2 处并退出码 1；还原后恢复通过。不是只跑了一遍绿的就算数。

### Run 7 自测记录（零 context 冒烟）

**怎么测的**

不是读文档判断，是**真的干净克隆**（`git clone` 到临时目录）、真的照 README 走、
真的在没有任何 API key 的情况下建一个新工程走到底。

**发现并修掉的 5 处卡壳**

1. **没有 key 就完全走不下去** —— 整条管线 audio-driven，没配音连 `build-vertical` 都起不来
   （退出码 4）；repo 里只有 `examples/` 有占位素材，且写死在那个例子里。
   → 新增 `lib/make-placeholders.sh`，任意工程都能造占位配音（时长按实测 6.35 字/秒算）+ 占位素材。
2. `init-project.sh` 打印字面量 `<repo>`，读的人得自己猜路径 → 改成打印真实绝对路径，
   并把「没 key 先跑占位」「素材目录约定」「审计」都列进后续步骤。
3. `shots.tsv` 模板固定 3 行，写了 5 句就 build 失败 → `make-placeholders --footage` 会补齐缺的拍。
4. README「30 秒上手」第 2 步写 `tests/validate.sh` 却注释成「检查依赖」（名实不符，
   而且它要跑好几分钟渲染）→ 改成 `check-deps.sh`，并补了「做第一条视频」的完整命令块。
5. **占位配音会静默通过 audit** —— `verify-audio` 只看时长和响度，正弦音全过。
   → 留 `audio/.placeholder` 标记，`audit-video.sh` 单独点名警告「绝不能交付」。

**常驻断言**

`validate.sh` 新增 4 条：init-project 不许打印字面量 `<repo>` / shots.tsv 缺行必须被补齐 /
无 key 必须能出 1080×1920 / audit 必须点名占位配音。以后这条路径再被弄坏会立刻红。

### Run 6 自测记录（质量闸门）

**跑了什么**

- `audit-video.sh` 在样例工程上真跑一遍：八项逐项出结论，
  画面构成算出「真实切片 37% / 卡片 36% / 产品录屏 27%」并正确提示产品占比不足 50%。
- 造了一条 640×360 的坏成片，audit 正确拦下（退出码 1）。
- `validate.sh` 新增 4 条断言：audit 通过并统计出画面构成、audit 必须输出人工清单、
  坏成片必须被拦、`build-vertical` 相对 `--out` 必须按 cwd 解析。

**揪出并修掉的 bug**

1. `build-vertical.sh` 的 `--out` 相对路径**被重复拼上项目目录** ——
   从 repo 根传 `examples/x/out.mp4` 会变成 `examples/x/examples/x/out.mp4`。
   之前没暴露是因为 `validate.sh` 一直传绝对路径。
   已改成按 cwd 解析（CLI 常规语义），并加了常驻断言锁住。
2. `audit-video.sh` 里两处 `$VAR` 后面紧跟全角括号 → `unbound variable`。
   **这次是 validate 的静态检查先报出来的**（Run 1 加的那条常驻检查），
   不是等到运行时才炸 —— 说明那条检查是有用的。

**样例工程按目录约定重摆了素材**（`footage/ext` / `footage/rec` / `html/beats`），
这样 audit 的画面构成统计在样例上就是有意义的，也顺带把约定教给读的人。

### Run 5 自测记录（动效卡）

**真渲染 + 抽帧目视，不是只跑通**

- 5 套模板各渲一张，全部 1080×1920，`--no-render` 模式也跑通（不依赖浏览器）。
- `shots.tsv` 5 行，格式直接能喂 `build-vertical.sh`。
- `validate.sh` 新增「卡模板 / storyboard」4 条断言（模板存在性、占位符无残留、
  shots.tsv 行数、5 张卡渲染尺寸与时长），全绿。

**抽帧目视揪出 2 个模板 bug（只跑通不看图是发现不了的）**

1. `question` 卡的输入框**动画结束后整体右移 410px**。根因：`.up` 动画的
   `to { transform: none }` 把 `.box` 的 `translateX(-50%)` 覆盖掉了 ——
   居中和动画抢同一个 `transform` 属性。改成用 `left/right` 定位。
   （这个 bug 原样存在于参考的真实卡片里，抄过来才发现。）
2. `bar-rank` 的参考线高度写死 `bottom:250px`，行数少时会拖到画面底部。
   改成 `{{rowsH}}`（行数 × 118px），由 `storyboard.js` 自动派生。

**量到的一个有用事实**

playwright 录出来的 webm 比请求的 `dur` **多 0.6–0.9s**（浏览器启停帧）。
方向是对的 —— 卡永远够长，`build-vertical.sh` 用 `-t` 裁到精确时长，不会黑屏。

### Run 4 自测记录（产品录屏）

**在真实录屏上跑的**

- `browser-chrome.py`：横版 1920×120 深色、竖版 1080×100 浅色各出一张，
  抽帧目视确认红黄绿灯 + 锁 + URL 药丸（域名白、路径灰）渲染正确。
- `zoom-crop.sh` 三种定位方式都在真实录屏上跑过：
  `--auto` 在 1080×1920 成品素材上正确检出内容区 `1032:844:24:372`；
  在 720×1280 满帧录屏上原样返回并给出「没有黑边可裁」的提示；
  `--zoom 1.5/2.0` + `--cy` 正常；`--grid` 导出 720×1280 的网格样帧（每格 72×128）。
- `wrap-chrome.sh`：横版（1920×120 壳 + 1920×960 内容）和竖版（1080×100 壳 +
  1080×1820 内容）各出一条，抽帧目视像真实浏览器窗口；
  素材不够长时默认报错、加 `--freeze` 后正确补到 12.00s。
- `validate.sh` 新增「产品录屏工具」9 条断言，全绿。

**量出来的新账（以前没算清楚）**

裁切倍数和升采样倍数**是相乘的**。目标 1080×1920 时：

```
录制宽 720  → 不裁就已经 1.50×；裁 1.5× = 2.25× ⚠；裁 2.0× = 3.00× ✗
录制宽 1080 → 不裁 1.00×；裁 1.5× = 1.50×；裁 2.0× = 2.00× ⚠
录制宽 1620 → 裁 1.5× 才刚好 1.00×
```

也就是说 **720×1280 的录屏根本不能再裁**——升到 1080×1920 就把预算用完了。
以前「录屏后裁 1.5–2× 放大」那条经验，前提是源本来就是 1920×1080；
套到移动端 720 录屏上会直接糊。已写成倒推表进 skill。

### Run 3 自测记录（真人切片混剪）

**在真实素材上跑的，不是只跑 fixture**

- `fit-vertical.sh` 拿真实素材库里**每一种画幅各跑一条**（1920×1080 / 1920×1062 / 1280×720 /
  960×720 / 640×480 / 640×360 / 480×360 / 448×336 / 480×852 / 360×640），
  10 条全部输出 1080×1920 / 3.00s / 无音轨，模式选择符合预期
  （横屏方形一律 blur；360×640 因为铺满要 3.0× 放大而回退到 blur，符合设计）。
- 抽帧目视三条输出：4:3 老档案、9:16 手机竖拍、16:9 —— 模糊背景 + 主体居中都正确，
  前景没有被拉变形，底边没有压到字幕线。
- `fetch-clip.sh probe` 在 5 条真实素材上跑过，av1 素材被正确标出。
- `fetch-clip.sh search` 走真网络跑过一次，正常返回候选。
- `validate.sh` 新增「fit-vertical 各画幅」5 条断言（4 种画幅 + 素材不够长必须被拦），全绿。

**contact 图当场抓出一条坏素材**

参考库里有一条 1280×720、标题写「数钞票」的免费素材，ffprobe 看一切正常
（h264 / 55s / 0.84× 降采样）。抽帧一看，9 格里只有 1 格是真的钞票画面且带水印，
其余是「ROYALTY FREE STOCK FOOTAGES」「CLICK TO DOWNLOAD」「DOWNLOAD IT BY THE LINK BELOW」
这类下载广告，还混着热气球、火焰、机场等无关镜头。
**ffprobe 一个都看不出来** —— 这条已作为真实案例写进 skill。

**修掉的 bug**

`fit-vertical.sh` 里 awk 的 `printf` 末尾漏了 `\n`，导致 `read` 返回 1，
在 `set -e` 下把脚本直接干掉 —— 表现是 10 条素材里只有走 `fill` 分支的那条出了片，
其余 9 条静默无输出。已修，并在代码里注了原因。

### Run 2 自测记录（选题与脚本）

**跑了什么**

- `validate.sh` 全绿，新增「脚本 linter」三条断言：真实交付稿必须放行（防阈值过严）、
  坏稿必须被拦、样例工程必须通过。
- 端到端渲染回归通过（1080×1920 / 11.35s / h264+aac / 30fps）。

**回测真实数据揪出的问题 —— 我上一轮自己定错了 3 条规则**

拿真实发过的稿子（`xuanxue-loop` v23，实测 52.31s）和 `laowai-zhun`（89.2s）回测：

1. **语速错了 27%。** 我写的是「5 字/秒」，实测 **6.35 字/秒**（含标点，presenter_male@1.30）；
   克隆音 @1.10 是 4.98。按 5 写的 75s 稿子实际只有 59s。已换成实测表，
   并注明留出法校验误差 8.8%（前 5 句拟合预测后 4 句）——估算只能到 ±10%，配完音必须实测。
2. **「单句 ≤30 字」是错的。** 真实交付稿 9 句里有 4 句超 30 字（43/61/37/60），
   横版稿甚至有 88 字的句子，全都正常交付。真正的约束是**一拍占多长画面**
   （实测最长一拍：竖版 10.04s / 横版 19.09s）。linter 改成按单拍时长判硬错误（>15s），
   字数只做提示（>45 字）。
3. **「末句 ≤15 字」是错的。** 实测末句往往是**全片最长**的一句（要把产品讲完，60 字）。
   改成检查「末句有没有收品牌」，不卡字数。

这三条都是我上一轮凭印象写的，回测才发现和实际交付内容矛盾。
`validate.sh` 现在有一条常驻断言：**真实交付稿必须被放行**，防止以后再把阈值定得比现实还严。

### Run 1 自测记录（2026-08-10 03:2x）

**跑了什么**

- `tests/check-deps.sh` —— 本机 ffmpeg 8.1.1 **无 libass**（已确认），字幕走 PIL overlay 是对的路线。
- `tests/validate.sh` 全绿：10 个 skill 结构 / 43 处内部引用无死链 / 35 个文件泄密扫描 / 16 个脚本语法 /
  端到端渲染出片并 ffprobe 校验。
- 额外手测：`install.sh`（装进临时 agent 目录，10 个 skill）、`init-project.sh`（建工程骨架）、
  `make-cover.py`（1080×1440 封面，抽帧看过版式正确）、`package-delivery.sh`（zip + 故事板，
  `ditto` 解压中文名正常）、`gen-subs.py --no-merge`（钩子拆三拍，时间轴首尾对齐 11.35s）。
- 抽帧目视：`celeb` 编码器的模糊背景+主体居中正确，字幕落在 Y=1400 下三分之一。

**这一轮真修掉的 bug**（都是自测跑出来的，不是想出来的）

1. `verify-audio.sh` 用了 `ffmpeg -v error` 跑 `volumedetect` —— 统计走 info 级日志，被吞掉后
   把正常音频全判成静音。已改 `-hide_banner -nostats`，并在解析不到时不瞎判。
2. `install.sh` 里 `$DEST（` —— bash 把全角括号的字节吞进变量名，报 `unbound variable`。
   已改 `${DEST}`，并把这条加进 `validate.sh` 作为常驻检查（又揪出 2 处同类）。
3. `package-delivery.sh` 用 `zip` 打包中文名不置 UTF-8 标志位 → 换解压器变乱码。改用 python
   `zipfile`（自动置位），实测 `ditto` 解压中文名正常。
4. `package-delivery.sh` 默认输出落在当前工作目录而不是成片旁边 —— 从别处调用会把 zip 丢错地方。已改。
5. `build-vertical.sh` 用了 `mapfile`（bash 4+），macOS 自带 bash 3.2 跑不了。已换 while-read。

### Run 1.5 自测记录（合规）

- `validate.sh` 新增三条断言全过：违规样本命中 8 条 BLOCK 被拦下（exit 1）、
  合规样本零误报（exit 0）、样例工程 `hello-vertical` 自身合规。
- **拿真实历史成片回测**（这才是词表有没有用的真检验）：
  `xuanxue-loop/clips.json`（AI 考命理）和 `deliver-tijian2/文案.txt`（命理体检 × 中医，
  医疗风险最高的一条）都是 **0 BLOCK / 0 WARN** —— 词表对真实内容没有误报。
- **回测发现的真问题**：过去的口播稿**基本没有框定话术**（xuanxue-loop 那条完全没有）。
  也就是说以前全靠选题本身安全，没有主动把结果定性为「参考 / 自我觉察 / 传统文化说法」。
  这条已写进 skill，linter 现在会以 INFO 提醒。

**已知不足（下一轮处理）**

- `rec-page.js` / `render-card.js` 只做了语法校验，**没跑过真实站点**（需要凭据 + 目标站）。
- `gen-voice.mjs` **没跑过真实 API**（需要 key）。逻辑按现役脚本移植 + 加了报错码提示。
- 语速表只有 2 个音色的样本；`female-tianmei` / `female-shaonv` 还没实测过。
